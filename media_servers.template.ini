[@MEDIA_SERVER_HOSTNAME@]
application = {default kLive}
port = {port used when playing in regular HTTP configuration}
port-https = {port used when playing in SSL configuration {optional, remove if not using}}
domain = @MEDIA_SERVER_HOSTNAME@
search_regex_pattern = {optional, remove if not using}
replacement = {optional, remove if not using}
